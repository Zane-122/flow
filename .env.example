PORT=3333
DATABASE_URL=postgres://postgres:postgres@localhost:5432/flow
SESSION_SECRET=change-me-to-a-long-random-string
DATA_DIR=./data
# Optional. First account can register with no invite if the users table is empty.
# BOOTSTRAP_INVITE=FLOW-XXXXXX
